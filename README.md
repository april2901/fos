[SE_Assignment2_G12.pdf](https://github.com/user-attachments/files/23809469/SE_Assignment2_G12.pdf)

# FoS (Focus on Speaking)

> LG Display 연동 실시간 회의 AI 프롬프터 프로젝트  
> 자세한 내용은 `latex.tex` 참고

---

## 🚀 로컬 실행 방법

### 한 줄 명령어로 프론트엔드 + 백엔드 모두 실행

```bash
npx vercel dev
```

✅ 이 명령어 하나로 프론트엔드(Vite)와 백엔드(API) 모두 실행됩니다.

---

## 📋 초기 설정 과정

아래는 내 실행예시, 비슷한 질문 나오면 참고하세요

```
npx vercel dev

Vercel CLI 48.10.0
? Set up and develop "~/Desktop/ai-assistant-for-presentation"? yes
? Which scope should contain your project? dennis04297-6907's projects
? Link to existing project? no
? What's your project's name? fos
? In which directory is your code located? ./

Local settings detected in vercel.json:
- Build Command: npm install && npm run build
- Development Command: npm run dev -- --port $PORT
- Framework: vite
- Output Directory: dist

Auto-detected Project Settings (Vite):
- Install Command: `yarn install`, `pnpm install`, `npm install`, or `bun install`

? Want to modify these settings? no
? Do you want to change additional project settings? no

🔗  Linked to dennis04297-6907s-projects/fos (created .vercel)
? Detected a repository. Connect it to this project? no

> Running Dev Command "npm run dev -- --port $PORT"
```

설정이 완료되면 브라우저에서 `http://localhost:3000` 접속